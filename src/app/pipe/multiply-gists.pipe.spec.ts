import { MultiplyGistsPipe } from './multiply-gists.pipe';

describe('MultiplyGistsPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyGistsPipe();
    expect(pipe).toBeTruthy();
  });
});
